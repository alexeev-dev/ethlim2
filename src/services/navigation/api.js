import {getQuery} from 'services/utils/api'

export const getTrafficSources = getQuery('navigation.trafficSources')
export const getPlatforms = getQuery('navigation.platforms')
export const getProjects = getQuery('navigation.projects')
